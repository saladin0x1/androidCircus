<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.MyApplication">

        <!-- Splash Screen (Launcher) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Sign In -->
        <activity
            android:name=".SignInActivity"
            android:exported="false" />

        <!-- Sign Up -->
        <activity
            android:name=".SignUpActivity"
            android:exported="false" />

        <!-- Home -->
        <activity
            android:name=".HomeActivity"
            android:exported="false" />

        <!-- Profile -->
        <activity
            android:name=".ProfileActivity"
            android:exported="false" />

        <!-- Doctor Home -->
        <activity
            android:name=".DoctorHomeActivity"
            android:exported="false" />

        <!-- Doctor Agenda -->
        <activity
            android:name=".DoctorAgendaActivity"
            android:exported="false" />

        <!-- Doctor Patients -->
        <activity
            android:name=".DoctorPatientsActivity"
            android:exported="false" />

        <!-- Patient Dossier -->
        <activity
            android:name=".PatientDossierActivity"
            android:exported="false" />

        <!-- Clerk Home -->
        <activity
            android:name=".ClerkHomeActivity"
            android:exported="false" />

        <!-- Create Appointment -->
        <activity
            android:name=".CreateAppointmentActivity"
            android:exported="false" />

        <!-- Forgot Password -->
        <activity
            android:name=".ForgotPasswordActivity"
            android:exported="false" />

        <!-- Reset Password -->
        <activity
            android:name=".ResetPasswordActivity"
            android:exported="false" />

        <!-- Clerk Approvals -->
        <activity
            android:name=".ClerkApprovalsActivity"
            android:exported="false" />
    </application>

</manifest>